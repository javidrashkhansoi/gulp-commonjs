git init
// git config --global user.name "Javid"
// git config --global user.email "cavidgulmaliyev491995@gmail.com"
git status
git add --all
git commit -m "first commit"
git branch -M main
// git remote add
git push -u origin main